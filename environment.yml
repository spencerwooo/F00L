name: adv
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3
  - tensorflow
  - numpy
  - matplotlib
  - pylint
  - yapf
  - notebook
  - jupyterlab=1.2
  - pytorch
  - torchvision
  - opencv
  - cudatoolkit=10.1
  - tqdm
  # downgrade scipy to accommodate inception_v3's slow load time
  - scipy=1.3
  - pip
  - nodejs
  - jupyterlab_code_formatter
  - python-language-server
  - pip:
      # foolbox 3.0.0 have some breaking changes
      # - foolbox==3.0.0b1
      - foolbox==2.4.0
      - ipywidgets
      - halo
